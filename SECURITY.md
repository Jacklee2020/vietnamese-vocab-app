# 安全说明（Security Policy）

## 项目定位

本应用是**纯前端、零依赖、完全离线**的 PWA：

- 无服务器、无后端接口、无第三方追踪脚本；
- 词库与学习记录仅保存在用户本机浏览器（localStorage / Service Worker 缓存）；
- 单词、进度、打卡数据**不会**上传到任何服务器。

## 报告漏洞

如果发现安全漏洞（如 XSS、数据注入、隐私问题），请**不要公开讨论**，直接私信仓库维护者，或通过 GitHub Issue（勾选「安全相关」）提交。提交时请附上：

1. 漏洞类型与影响范围；
2. 复现步骤（尽量附带截图）；
3. 建议的修复方式（可选）。

维护者会在收到报告后尽快评估并修复。

## 安全承诺

- 词库 CSV 支持用户导入导出，所有外部文本只按纯文本渲染，不执行任何脚本；
- 任何新功能（如云端同步）上线前都会经过安全审查，并保持「默认离线、数据不出本机」的原则。
